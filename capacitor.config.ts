import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'challenge-eldar-frontend',
  webDir: 'dist/challenge-eldar-frontend/browser', 
};

export default config;
